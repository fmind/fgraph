# fgraph (Python implementation)

The Python twin of fgraph. Not yet scaffolded: the implementing agent creates the uv project here per `SPEC.md` §15 M0, following `~/.agents/skills/python-stack/SKILL.md` (library + CLI profile, distribution `fgraph`, `src/fgraph/` layout, Typer CLI, stdlib-only core, optional `fgraph[vector]` extra for NumPy).
