# fgraph (Go implementation)

The Go twin of fgraph. Not yet scaffolded: the implementing agent creates the module here per `SPEC.md` §15 M0, following `~/.agents/skills/go-stack/SKILL.md` (module `github.com/fmind/fgraph/go`, package `fgraph` at the module root, `cmd/fgraph/` CLI with urfave/cli/v3, `modernc.org/sqlite` driver, CGO-free).
