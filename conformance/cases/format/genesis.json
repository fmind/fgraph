{
  "name": "format/genesis",
  "comment": "A freshly initialized database contains exactly the 25 genesis facts (SPEC §4.5): the bootstrap is self-describing and queryable through the normal API, and no user rows exist.",
  "steps": [
    {
      "q": { "find": ["?d"], "where": [["fgraph/at", "fgraph/doc", "?d"]] },
      "expect": { "columns": ["?d"], "rows": [["Wall-clock time of the transaction (UTC microseconds)."]] }
    },
    {
      "q": { "find": ["?t"], "where": [["fgraph/excised", "fgraph/type", "?t"]] },
      "expect": { "columns": ["?t"], "rows": [["ref"]] }
    },
    { "facts": true, "expect": [] }
  ]
}
